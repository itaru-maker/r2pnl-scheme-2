|def 'not (x) (
   |if x
      #false
      #true
   ;
);

|let 'count 0;
|while (|not |= 10 count;) (
   |write count;
   |set 'count |+ count 1;
);


|repeat 10 (
   |display "hello";
   |newline;
);


|repeat 33 (
   |display "エディタは、emacsがおすすめです。（emacserの声）";
   |newline;
);
